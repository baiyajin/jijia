<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.baiyajin.pagedata.mapper.PageSubscriptionMapper">

    <!--分页查询报告-->
    <select id="findList" parameterType="com.baiyajin.pagedata.vo.SubscriptionVo" resultType="com.baiyajin.pagedata.vo.SubscriptionVo">
        SELECT
        a.id as "id",
        a.number as "number",
        a.title AS "title",
        a.createTime AS "createTime"
        FROM page_subscription a
        WHERE
        a.statusID = "qy"
        <if test="number !=null and number != ''">
            AND a.number = #{number}
        </if>
        ORDER BY createTime DESC
    </select>

</mapper>